plugins {
    id 'java'
}

version 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    implementation(
            project(':application:web-app:react-client')
    )
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
